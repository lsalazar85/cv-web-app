import styled from 'styled-components'

const Card = styled.div`
    flex: 1;
    border-bottom: 1px solid #353535;
    padding-bottom: 24px;

    &:last-child {
        border-bottom: none;
    }
`

export default Card